- name: Varaible from inventory files
  hosts: local
  connection: local
  tasks: 
  - name: Course info
    ansible.builtin.debug:
      msg: "Course is {{ COURSE }}, it takes {{ DURATION }} and learner is {{ LEARNER }}"