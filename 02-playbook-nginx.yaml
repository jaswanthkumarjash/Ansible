- name: install and start nginx
  hosts: inventory
  become: yes
  tasks:
  - name: install
    ansible.builtin.package:
      name: nginx
      state: present
  - name: start nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes